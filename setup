# Install cygwin
#
# vim, git, svn, zsh, screen, ssh, ??

# download my .zshrc
wget http://dotfiles.org/~bryndin/.zshrc -O ~/.zshrc

# VIM plugins
  mkdir -p ~/.vim/autoload ~/.vim/bundle

  # Pathogen
  wget 'www.vim.org/scripts/download_script.php?src_id=16224' -O ~/.vim/autoload/pathogen.vim

  # Solarized Colorscheme
  cd ~/.vim/bundle
  git clone git://github.com/altercation/vim-colors-solarized.git
  
wget https://github.com/spf13/spf13-vim/raw/master/.vimrc  -O ~/.vimrc --no-check-certificate

